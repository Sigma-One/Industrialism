package sigmaone.industrialism.item.tool

import net.minecraft.item.PickaxeItem
import net.minecraft.item.ToolMaterial

class ToolPickaxe(material: ToolMaterial?, attackDamage: Int, attackSpeed: Float, settings: Settings?) : PickaxeItem(material, attackDamage, attackSpeed, settings)