package sigmaone.industrialism.item.tool

import net.minecraft.item.SwordItem
import net.minecraft.item.ToolMaterial

class ToolSword(material: ToolMaterial?, attackDamage: Int, attackSpeed: Float, settings: Settings?) : SwordItem(material, attackDamage, attackSpeed, settings)